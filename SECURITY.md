# 安全策略

请不要在公开 Issue 中披露可被利用的安全细节。可以通过 GitHub Security Advisory 的“Report a vulnerability”私下报告。

当前仅维护最新 GitHub Release。自动更新只接受本仓库 Release 中同时存在的 `AIFrontier-Setup.exe` 与 SHA-256 校验文件。首次安装请只使用本仓库的 Releases 页面；项目尚未配置商业代码签名证书。
